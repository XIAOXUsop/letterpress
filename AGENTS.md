# 知识库维护约定

> 这份文件是**写给 AI agent 看的**，不是写给人看的。
> 它对应 Karpathy 的 LLM wiki 模式里的「schema 层」——定义这一层怎么维护。

如果你是一个 agent，被要求在这个仓库里写文章或整理知识，先读这份文件。

---

## 这个仓库是什么

一个静态博客。构建产物是纯静态文件，没有数据库，没有后端。

内容分两类，**组织方式不同，地位相同**：

| | 位置 | 组织方式 | URL |
|---|---|---|---|
| 文章 | `src/content/posts/` | 按时间排（流） | `/<slug>/` |
| 知识层 | `src/content/wiki/` | 按主题互链（网） | `/wiki/<slug>/` |

**不要用文章做知识整理。** 文章是写给「顺着时间读」的人的；
知识层是写给「查某个概念」的人的。混淆两者会让两边都不好用。

---

## 写一篇新文章

在 `src/content/posts/` 下新建 `.md`，文件名就是 URL 片段（用英文小写连字符）。

```markdown
---
title: 标题
summary: 一句话摘要。必填，它会进 llms.txt、meta description 和列表页
date: 2026-01-15
tags: [标签一, 标签二]
---

正文用 markdown。
```

**`summary` 不能省。** 它是 agent 判断「要不要读这一页」的唯一依据。

未写完的加 `draft: true`——草稿不进构建、不进列表、不进 llms.txt，
也不会进入 `content-manifest.json` 或 `content.ndjson` 机器出口。

---

## 写一个知识层条目

在 `src/content/wiki/` 下新建 `.md`。

```markdown
---
title: 概念名
summary: 一句话说清它是什么
kind: concept        # concept | entity | synthesis
related: [其他条目的-slug]
# 依据从哪来。**两种写法，二选一**：
# sources: [{ sourceId: ..., revision: ..., locator: ... }]   # 外部规范/研究
# original: { reason: 为什么没有外部来源 }                   # 本站自己的实践
review:
  status: reviewed    # pending | reviewed | stale
  checkedAt: 2026-01-15
  contentDigest: …    # 跑 npm run wiki:review -- --slug <slug> 取值
---

正文。
```

三种 `kind` 的用途：

- `concept`——概念、方法、模式。**这是默认值**
- `entity`——具体的人、工具、项目
- `synthesis`——跨条目的综合性结论

### 「没有来源」有两种含义，所以要显式区分

一篇知识页**不登记来源**，可能是两件完全不同的事：

| | 例子 | 含义 |
|---|---|---|
| **该登记却漏了** | 「规范说 `ch` 等于 0 字形」 | ❗ 缺依据，必须补 `sources` |
| **本来就没有外部来源** | 「本站的强调色选了 `#002FA7`」 | ✓ 写 `original` 说明理由 |

不写 `original` 的话，两种**在数据里长得一模一样**，
复核的人无从判断「没有来源」是该补还是正常。

```yaml
original:
  reason: 本站的设计选择与内部约定，外部规范没有对应条款
```

`reason` **必填**——空理由等于没声明。

> `original` 与 `review` 是**两件独立的事**：
> `original` 说「依据从哪来」，`review` 说「确认过没有」。
> **本站自己的选择同样需要有人复核过**，否则它只是一句声明。

`related` 是显式的关联声明，会参与链接图。
**但更推荐在正文里写 `[[链接]]`**，因为读者能看到上下文。

### ⚠️ 不要把同一关系写两遍

正文里 `[[设计令牌]]` 和 frontmatter 里 `related: [design-tokens]`
**指的是同一个页面**（一个写标题、一个写 slug 也一样）。

两处都写**不会让构建失败**——链接图会去重，边不会重复、计数不会错。
但代价是：**改名时要改两个地方，漏掉一处就断链。**

2026-09-24 实测过这条代价：做改名实验时改完正文 `[[...]]` 才发现
`related` 里也有同一个目标，于是构建被 `broken-wikilink` 拦下，
而在那之前**没有任何检查提醒过这里有重复**。

现在 `lint` 会报 `redundant-relation`（警告级）。
**看到它就删掉 `related` 里的那一项**——正文链接是权威的那一份，
因为读者能看到它，也因为它带着上下文。

`related` 留给**正文里没有链接、但确实相关**的页面。

---

## 链接语法

```
[[目标]]              → 链接，显示文本即目标名
[[目标|显示文本]]      → 自定义显示文本
[[目标#小节]]          → 指向页内某小节
```

目标可以写 slug，也可以写**标题**。解析时大小写和空格都不敏感。

代码块与行内代码里的方括号**不会被解析**——讲解语法的文章可以放心写。

### 断链会让构建失败

引用一个不存在的目标会报 `broken-wikilink`（错误级），构建中止。

**这是刻意的，不要绕过它。** 断链会让「知识库」退化成「一堆文件」——
而这是这个项目存在的理由。正确做法是二选一：

1. 新建那个条目
2. 把引用改成已存在的条目

不要为了让构建通过而把 `[[x]]` 删掉或改成纯文本——那是在掩盖问题。

---

## 每次构建会跑体检

输出在控制台，分三级：

| 级别 | 规则 | 含义 |
|---|---|---|
| 错误 | `broken-wikilink` | 引用了不存在的页面。**会让构建失败** |
| 错误 | `duplicate-slug` | 两个文档抢同一个 URL，后构建的会静默覆盖先构建的 |
| 错误 | `reserved-post-slug` | 文章占用了 `/about/`、`/wiki/` 等系统路由，HTML 会被静默跳过 |
| 警告 | `orphan-page` | 没有任何页面指向它。读者只能靠直接输 URL 找到 |
| 警告 | `missing-summary` | 缺摘要，agent 只能靠标题猜内容 |
| 警告 | `empty-body` | 空正文。没写完就加 `draft: true` |
| 警告 | `redundant-relation` | 同一关系在正文 `[[链接]]` 与 `related` 里各写了一次。不影响构建，但改名时要改两处 |
| 提示 | `summary-too-long` | 摘要过长，搜索引擎会在 meta description 处截断 |
| 提示 | `cjk-slug` | URL 由中文标题生成（默认关闭，可在配置里开） |

**孤儿页是最值得注意的一类。** 一个没有任何入链的条目，
在实际阅读路径里等于不存在。写新条目的同时，**应该从相关的既有条目里链过去**。

---

## 你可以做而机械检查做不到的事

自动体检只覆盖**机械可判定**的部分。下面这些需要你来判断：

1. **发现矛盾**——两个条目对同一件事的说法不一致。发现了就写进正文并标注。
2. **发现过时**——某条结论已被新的事实推翻。改掉它，或明确标注日期与失效条件。
3. **建立新连接**——读完一篇新文章后，判断它和哪些既有条目相关，加链接。
4. **拆分过长的条目**——一个条目讲了三件事时，拆成三个并互相链接。
5. **合并重复条目**——两个条目讲的是同一件事时，合并。

**做这些时不要只改内容，要同时维护链接。** 知识库的价值来自连接，
一个内容正确但孤立无援的条目，价值远低于一个内容一般但被多处引用的条目。

---

## 不要做的事

- **不要动 `src/lib/` 和 `src/styles/`**，除非用户明确要求改功能或样式。
  那是引擎，不是内容。
- **不要为了让检查通过而删除内容。** 断链要修，不是要删。
- **不要发明新的 frontmatter 字段。** schema 在 `src/content.config.ts` 里，
  加了字段要同步改那里，否则会被静默忽略。
- **不要在文章里做知识整理。** 见本文开头。

---

## 验证你的改动

```bash
npm run build     # 会跑体检，有错误会中止
npm test          # 单元测试
npm run verify    # 端到端验证（含 wiki 链接在产物里真的被渲染）
npm run verify:all   # 提交前跑这个（它自己会逐步打印每一步）
```

> ⚠️ **别手写「几道门禁」这个数。** 它随门禁增删变化，而本文件里这个数
> **漂过不止一次**（曾写 10、11、13、17）。
> 权威名单是 `scripts/check-gate-list.mjs` 里的 `EXPECTED`——
> 门禁增删时**它会红**（`verify:gates` 是 `verify:all` 的第 2 步，
> 一有差异就立刻报「多出来的：…」与「期望 N 步」）。
> 本文件这一行是转述，**红了就照 `EXPECTED` 改它**。

### 要更新 README 里的实测数字时

```bash
npm run measure    # 产物实测；没有 dist 会自己先 build
```

**别手抄，也别自己重新量。** 这个命令的口径**就是门禁的口径**
（`Math.round(bytes / 1024)`、gzip level 9、只用字节数）。

> 2026-09-24 一天之内，因为「每次重新发明量法」而量错四次：
> `du -sk` 按 4 KB 块对齐、`ceil(bytes/4096)` 与块对齐是两个算法、
> `int()` 截断 vs `round()`、以及**写下一个自己没查过的文件类型**（wasm）。
> 四个数都是「实测」，而其中三个是错的。
>
> **量法本身是代码，就该复用而不是重写。**

`npm run verify` 里有一条契约：**正文里不应残留未解析的 `[[方括号]]`**。
这条是补上来的——曾经单元测试全绿而所有链接都没渲染出来，
原因有两层且都静默：Astro 的 markdown 处理器换过 API，以及内容层有缓存。

改了 `astro.config.mjs` 里的 markdown 配置后，**必须 `npm run clean`**，
否则内容层的缓存会让你以为改动没生效。
