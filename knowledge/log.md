# 维护记录

按时间倒序。每条记录一次「来源 → 影响清单 → 修订 → 复核 → 发布验证」的完整过程。

工具还没建（那是 M1 之后的事），所以这里都是**手工跑通的**——
先证明这条路走得通、走得值，再考虑把它自动化。

---

## 2026-09-24 · 迭代 G：把 `wiki:impact` 从「没人验过的脚本」变成有测试的模块

阶段 3 的第一条退出条件是「**对预埋的显式来源变更，直接影响召回率为 100%**」。
去看实现，发现 `scripts/wiki-impact.mjs` **零测试**——
`verify.test.ts` 只是把它当作「这个文件存不存在」的一个素材，
从来没验过它算得对不对。

**而一个没人量过的召回率不是一个数字，是一个愿望。**

### 根因不是「忘了写测试」，是「没法写」

那份计算整个写在脚本顶层：读文件、算影响、打印，全在一起。
**逻辑无法 import，就无法单测**——这不是疏忽，是结构决定的。

所以把它抽成 `src/lib/wiki/impact.ts` 的纯函数 `computeImpact`，
脚本只保留读文件与打印。**两边共用同一份实现**——
复制一份到测试里再验一遍等于验了个副本，副本会漂，而漂了没人知道。

新增 14 条测试，其中直接对着阶段 3 那条退出条件写：

```
预埋 4 页引用 src-a，另有 1 页引用别的来源、1 页无引用
断言：漏召回为空 **且** 误召回为空
```

**两条都要**。只查召回，一个「把所有页面都返回」的检索器能拿满分。

### 把「约定」改成「断言」

原脚本里有个内联的 `if (重叠了) continue`，靠写代码的人记得加。
而重叠的后果是读者以为「还有别的地方要改」——**恰好是这个工具要消除的困惑**。

改成 `isDisjoint()` 断言，重叠直接 `exit 1`。

**断言写了不等于它会红，所以做了负向验证**：把那句 `continue` 删掉再跑，

```
退出码 = 1   （负向验证，去掉过滤后）
退出码 = 0   （恢复后）
```

顺带一个过程教训：我第一次接上共享实现时**顺手把过滤也删了**，
结果断言立刻报错——**报错是对的，是我的补丁不完整**。
先看到红再确认设计，比先相信设计再验证要可靠。

### 门禁第三次抓到我

399 项测试 → `verify:testcount` 报出四处 README/docs 写的 385。已同步。
本轮三次门禁拦截（token 表、测试条数 ×2），**每次都是真的**。

### 未决

1. **`wiki:impact` 仍不进 `verify:all`。** 它是人工触发的只读命令，
   阶段 3 要的「召回率 100%」现在由**单测**保证（14 条），
   但**真实内容**的召回率还没有门禁在量。
   要加的话需要一个「预埋来源变更 → 断言直接引用者集合」的脚本级检查。
2. **posts 层的来源治理仍未开始。** `wiki:impact` 只扫 `src/content/wiki/`，
   所以迭代 D 登记的 `ahrefs-llmstxt-study` 直接引用者是 0——
   它只在 ③ 组以文件路径出现。**这不是漏报，是 posts 还不参与来源模型**。

### 验证

`npm run verify:all` 13 步全过；399 项单测、196 项契约、107 文件可复现构建全绿。

---

## 2026-09-24 · 迭代 E：稳定身份，以及一次「看起来更对」的改法被实测否掉

路线图阶段 1 的退出条件里有一条：**「每个公开文档只有一个规范 ID 和一个当前 URL」**。
去看实现，发现 `manifestId` 就是 `${kind}:${slug}`——**ID 完全是 URL 的函数**。

后果具体是什么：manifest 是**对外发布**的机器出口，下游按 `id` 同步。
改一次文件名或显式 slug，同一篇内容的 ID 就变了，
订阅者会把它当成一篇新文档，旧文档凭空消失且无从追溯。

而这条**此前没有任何测试覆盖**——`content-manifest.test.ts` 里一条 `manifestId` 用例都没有。
这就是它能长期错着的原因：**没人量的东西不会坏，只会不变。**

### 先试了「内容寻址」，被自己的用例否掉

第一版实现是「没有显式 `id` 时，用 `kind` + 规范化正文摘要的哈希」。
理由听上去很对：正文没变，改名就不影响 ID。

**然后新写的用例直接把它打红了**：

```
FAIL 不同文档不会撞 ID
AssertionError: expected 'post:a1845325f951c9f4' not to be 'post:a1845325f951c9f4'
```

两篇 fixture 的正文都是 `'正文'`，于是**得到同一个 ID**。
这不是 fixture 的问题——**正文相同的两篇不同文档在真实站点里完全可能**，
而 manifest 的 ID 是下游认文档的唯一凭据，**撞 ID 意味着两篇互相覆盖**。

**这比「改名会改 ID」严重得多**，所以这条设计被否掉，回到「显式 id 优先、缺省仍用 slug」。

> **一个更隐蔽的问题我当时没看出来**：内容寻址下**改一个字正文就换 ID**。
> 那等于每次修订都让全体下游重新同步——比改名频繁一个数量级。
> **「更稳定」的方案在这里反而制造了更大的不稳定。**

### 顺带修掉我自己写错的一条断言

新加的用例里我一度写了「不同 slug 也必须得到相同 ID」。
**那是错的**——那等于允许两篇不同内容共用一个身份，正是 manifest 要防的覆盖。
写完读一遍才发现：**它和同一个 describe 里「不同文档不会撞 ID」直接矛盾。**
断言自己内部打架，说明它测的不是一个想清楚的要求。

最终形态：显式 `id` 优先；缺省仍退回 `kind:slug`（**不制造新的破坏**），
并把「没写 id 的文档改名仍会改 ID」这个边界**写在代码注释里**，而不是假装解决了。
schema 两个集合都加了 `id` 字段（**不声明会被 Zod 静默剥离**，见 `content.config.ts` 里 draft 那段注释），
六个 wiki 条目全部写上了显式 id——**知识条目被改名/合并的频率比文章高**，
所以这个字段对 wiki 更值得写。

### 改名实验：ID 确实稳住了，但改名的**成本比想象中高**

不满足于单测绿，实际改了一次名来做证伪实验：把 `design-tokens` 的 slug 改成
`design-tokens-renamed`。结果 ID 仍是 `wiki:design-tokens`，URL 变成
`/wiki/design-tokens-renamed/`——**身份与 URL 成功分离，机制成立**。

但实验过程本身比预想的更有价值：**改名连续撞了三道门**。

1. **两处正文 `[[design-tokens]]` 断链**，构建中止。
2. 改完正文才发现**真正的断链在 frontmatter 的 `related`**——
   `related: [cjk-web-typography, design-tokens]` 里也有它。
   也就是说**一次改名要改三类地方**：正文链接、`related` 声明、以及别处提到它的散文。
3. 修完断链，又被 **`review-stale`** 拦下——改动了正文，
   复核摘要自然对不上。

> **这说明「稳定身份」只解决了 ID 漂移，没解决改名本身。**
> 而且第 2 条是我**改完第一轮才发现的**：第一轮我只搜了 `[[...]]`，
> 没想到 `related` 也是一条引用通道——**同一个概念有两套写法，
> 搜的时候只搜一套，就会漏**。这和 `log.md` 里记过的
> 「frontmatter 有两套解析」是同一族问题：**通道不止一条。**

**教训**：`related` 与 `[[wikilink]]` 在功能上重叠，重复带来的是
「搜引用时容易只搜一处」的成本。真要收敛，应该让 `related` 由
正文链接派生，而不是要求作者两处都维护。**本轮不做**——
那会改 `content.ts` 的取图逻辑，属于路线图阶段 1「统一解析」的范围，
不在这一小步里。

### 门禁又抓了一次漂移

加完 6 条测试后 379 → 385，`verify:testcount` 立刻报出四处 README/docs 写的 379。
已同步。**这是同一个门禁本轮第二次抓到我**（上一次是 token 节省率）。

### 未决

1. **posts 还没写显式 id。** 只给 wiki 六页写了。文章改名的频率低，
   但这意味着**文章的 ID 仍随 slug 变**。要不要全量补齐，
   取决于是否真有外部订阅者——**在有真实消费方之前不补**，
   补了也只是让 11 个 ID 变一次，没有收益。
2. **改 ID 规则会让全体 ID 变一次**，等于让下游全部重新同步。
   manifest 有 `version` 字段就是为此准备的，但**本轮没有升 version**
   ——因为最终实现保持了缺省行为不变，产物里的 ID 一个都没变。

### 验证

`npm run verify:all` 13 步全过；385 项单测、196 项契约、107 文件可复现构建全绿。

---

## 2026-09-24 · 迭代 D：把「上一轮验收说已修」的四项逐一重验

**这次不是修新问题，是回头验上一轮的结论。**
`letterpress-llm-wiki-M0验收与下一步方案.md` 判定 M0「不可关闭」，
列了 4 个 P1、2 个 P2。而 `log.md` 里 2026-09-22 那条 M0 记录的「未决问题」
同样挂着其中几项。**两份文件对同一件事的判断不一致。**

我按「文档写的是已修，还是真已修」逐项去核，结论是：
**W3C 固定版本确已收口（14 处引用全是 `WD-…-20240312` / `WD-…-20260913`），
其余四项原封不动。** 已全部修订。

### ① 「10–15 个页面」—— 上一轮说「没有出处」，其实有

抓 `gist.githubusercontent.com/…/raw` 逐字核对，Ingest 段落写着：

> A single source might touch 10-15 wiki pages.

**所以「没有出处」是错的。** 已在 `llm-wiki.md` 勘误，并给
`llm-wiki-gist` 登记了带 `evidence` 的定位版本 `gist-2026-04-ingest`。

**但要修的另一半仍然成立**：紧接的下一句是作者描述自己偏好的工作方式
（一次一个、保持参与），属**个人经验举例**，不可泛化、不作 KPI。
**「有出处」与「可泛化」是两件事。**

> **金标自己也会过期。** `knowledge/questions.md` 里那条问句的说明写着
> 「它没有出处、本轮也没复现过」——**金标会去检索一句页面上不存在的话**，
> 于是它量的东西已经不存在了。改事实就必须改金标，这两处是同一个事实的两个副本。

### ② Ahrefs —— 上一轮「查不到就删掉」，删对了理由、错了结论

2026-09-23 那一轮把 137,210 / 97% / 96% 整组数字从文章里删掉，
理由是「按几种常见路径试 `ahrefs.com/blog/*` 全部 404」。
**那个推理是对的，结论是错的**：原文一直在
`https://ahrefs.com/blog/llmstxt-study/`，只是没试中路径。

2026-09-24 抓原文核对，数据全部对得上（137,210 个 2026 年 5 月有流量的域名、
28% 发布 llms.txt、97% 零请求、有请求的 3% 里 96% 来自 bots）。
已补回正文 + 参考文献 + `ahrefs-llmstxt-study.json` 登记。

**顺带修掉两处由此产生的过期陈述**：

- 原文发布于 **2026-06-15**，统计窗口是 **2026 年 5 月**——先前把两者混成了「2026 年 5 月实测」。
- `google-ai-features.json` 的 `_comment` 写着「替代那条核不掉的 Ahrefs 引用」。
  两者**不是替代关系**：Google 说的是「不需要额外文件」，Ahrefs 说的是
  「llms.txt 实际很少被请求」，**问的不是同一个问题**。已改写。

**教训**：因为「试了几个路径都 404」就判定来源不存在，是把
**「我没找到」当成了「它不存在」**。这两个 404 只证明了前者。

### ③ `em` 与汉字容量 —— 上一轮改了算式，没动那句更大的话

`em` 等于**计算后的 font-size**，规范保证且与字体无关。
但**「一个汉字正好 1em 宽」不是规范保证** —— 汉字 advance 取决于具体字体。

**决定性证据是规范自己**：CSS Values 4 专门定义了 `ic` 来表示
「字体中全角字形的典型 advance」，并规定**无法确定时假设为 1em**。
原文已抓到。**如果 `em` 就能保证汉字宽度等于字号，规范不必再定义 `ic`。**

原文还留着三句越界的话，全改了：

- `posts/cjk-web-typography.md`：「一个汉字约 1em 宽……**而且不随字体变**」
- `wiki/cjk-typography.md`：「粗估 `66ch` 是粗估，**`em` 不是估算`**」← 最直接的一句
- `tokens.css` 注释 + `docs/cjk-typography.md`：「能写成**确定的换算式**」

改后的表述统一为：**保证的是容器宽度稳定跟随字号，不是每行字数确定。**

### ④ 苹方字重 —— 与 Apple 官方清单直接冲突

`base.css` 写着「苹方、微软雅黑只有 400 与 700，写 600 会落到 700」，
而同一篇文章第 31 行承认「苹方有 Ultralight 到 Semibold」——**自相矛盾**。

抓 Apple 官方系统字体清单核对：PingFang SC 下**确有 `PingFang SC Semibold`**
（连 TC 也有）。已登记为 `apple-system-fonts.json`。

**更重要的是把「最接近」的方向查准了**。§2.2.2 只说
`a face with a nearby weight is used`，精确定义在 §5 的字重匹配算法：

> If the desired weight is greater than 500, weights greater than or equal to the
> desired weight are checked in ascending order followed by weights below…

**即请求 600 时先向上找（600→700→800→900），找不到才向下。**
所以有真 600 的字体**直接命中 600**，只有 400/700 的才落到 700。
**上一轮把它当成对所有中文字体都成立的规则，是把条件句套成了普遍句。**

### 门禁抓到的第五件事：token 表漂了，而它有三个副本

内容改完跑 `npm run verify`，契约红了两条：README 与 `docs/content-negotiation.md`
里的节省率还写着 **61.5% / 61.1%**，实测已经是 **57.7% / 55.8%**。

原因是这次给两篇文章补了规范原文（`ic` 的定义、字重匹配算法），
**新增文字在 HTML 与 markdown 孪生里都在**，分母分子同时变大，节省率必然下降。
与 2026-09-22 那次**机制完全一样**——所以它是内容维护的常态成本，不是回退。

**这一条真正值得记的是「我差点怎么算错」**：

我先照着 `estimateTokens` 的注释用 Python 重写了一遍算法去复算，
得到 6851/2944/57.0%——**和契约的 6750/2852/57.7% 对不上**。
差在哪？**我复现的是「我以为的算法」，不是项目里那个函数。**
真实值也一并偏了。

于是改成让项目自己算：Node 里 import `estimateTokens` 跑产物，
Python 只负责对**同一批字符串**做 `o200k_base` 编码。
两边这才对齐（6750/2852 ↔ 7530/2965）。

> **手抄数字必然漂；「照着注释重写一遍算法」比手抄更隐蔽——**
> 它看起来是在独立验证，实际验证的是我自己读代码读得对不对。
> 这仓库里已经有两条同族记录（177→180 项契约、4728→5164 的 token 表），
> 这是第三次。**正确做法是复用被测的那个函数本身。**

**顺带发现第三份副本**：`src/lib/negotiate/accept.ts` 的注释里也抄着同一组数字，
而且它那儿的第三页写 `3681 / 3963`，与文档表里的 `3,958` **本来就不一致**——
两份各写各的，**没有任何检查比对它们**。已一并同步，并在三处都写明
「这张表量的是当次构建的产物，内容一改就过期，别手抄」。

### 未决

1. **跨平台实际渲染仍未实测。** 上面这些是**规范与官方清单**层面的结论，
   具体浏览器在具体系统上落到哪一档，本项目**没有实测**。已写明，不再声称。
2. **「0 字形约 0.5em」仍是未实测假设**，`66ch ≈ 33em` 因此是推算。已在三处标注。
3. **真实 tokenizer 复算仍是手工跑的**（`tiktoken` 不在依赖里）。
   本轮它抓到的不是自己漂，而是**两份手抄副本互相对不上**。
   要根治得让契约自己去比对——但那需要引入 tokenizer 依赖，
   与「零依赖、可离线、确定性构建」的立身之本冲突。**这是个真实的取舍，不是遗漏。**

### 验证

`npm run verify:all` 全绿（详见当次运行）。类型检查 109 文件 0 错误。

---

## 2026-09-23 · 可证伪声明：把「页与仓库之间」的陈旧变成 CI 能查的东西

**起因是一次实测**：`llm-wiki.md` 的「已实现 / 计划实现」表里，
**三行写着「计划中，尚未实现」，而它们全都已经落地**
（`knowledge/sources/*.json`、`Doc.sources` / `Doc.review`、
`scripts/wiki-impact.mjs`）。写完那句话的时候它们就存在了。

没有任何检查会发现——lint 管的是断链、重复 slug、孤儿页，
那些都是**页与页之间**的问题。而这一条是**页与仓库之间**的问题。
**它读起来像事实，所以没人会去核实。**

### 做出来的东西

`verify:` 声明写在知识页 frontmatter 里，**每条查两件事**：

```yaml
verify:
  - claim: 来源登记与版本固定
    stated: 来源登记与版本固定 | **已实现**
    path: knowledge/sources/*.json
    expect: exists
```

- `stated`——页面正文里那句原话，必须**逐字**出现；
- `path` / `expect`——那句话对应的文件在不在。

**两个都查，缺一不可。** 只查文件的话，正文改了、声明没改时，
声明还在验一句没人说过的话——检查照样绿，而它绿得毫无意义。

放在 `astro.config.mjs` **加载期**，理由与 frontmatter 校验完全一样：
在 remark 管道里抛错，Astro 不会让构建失败（实测：错误打印 22 次、
页面退化、而 `Build exit code: 0`）。

### `globToRegExp` 被写坏了三次——这是本轮最值得记的一段

这个机制里唯一一处自己写的解析，三次的症状**都是「正则看着挺对、而且测试是绿的」**：

| 版本 | 写法 | 症状 |
|---|---|---|
| ① | `**` → `(?:[^/]+/)*/`，又叠加循环补斜杠 | 要求**至少一层**子目录，`src/config.ts` 匹配不上 |
| ② | 「上一段是 `**` 就不补斜杠」 | `src` 与 `**` 之间那个斜杠丢了，**全都匹配不上** |
| ③ | 分隔符补在 `**` 分支之后 | `src/**` 正常了，`src/**/*.ts` 又全不中 |

三次都是**我盯着 `.source` 数斜杠**，而 `new RegExp` 的 `.source`
会对 `/` 补反斜杠——数出来的是 `\/`，肉眼极容易多数或少数一个。
所以现在测试里的断言**一律用 `test()`**，不碰 `.source`，
并且拿**真实文件树**交叉验证（手写的期望值可以跟实现一起错）。

那个 glob 正好写在知识页的示例里——**金标全绿、功能是坏的**，
本仓库最防的那类 bug，这次踩在自己身上。

### 负向验证抓到一个真缺陷

第一轮注入「把表改回『计划中』但不改声明」时，**检查没红**。
原因：`evaluateClaims` 收到的是**整个文件**，而 `stated` 的字符串
**在 frontmatter 里它自己那一行就有**——**声明在验证自己的字符串**。
改成只喂正文之后，注入立刻变红。

另外两轮注入（改文件路径、反转 `expect`）都正常变红。

### 未决

1. **只有 1 个知识页有 `verify:` 声明。** 其余 5 页还没写——
   而写声明要先把页面里的可证伪说法找出来，那是一次内容审阅。
2. **它只查「文件在不在」，查不了「写对了吗」。** 一条声明能覆盖的范围
   恰好就是「有没有」这个层级，写清楚比写宽重要。
3. `globToRegExp` 的 `**` 只支持「零个或多个目录」这一种语义，
   没有 `**` 出现在段中间的形态（如 `a/**/b/**/c`）的测试——

---

## 2026-09-23 · 检索可评测：context pack + 19 条金标

**动机**：路线图 §15 把「检索可评测」排在「Agent 可安全消费」之前。
在此之前，「检索能用吗」这个问题**没有答案**——每次改分词、改权重，
能得到的结论只有「我试了几个查询，看着还行」。

### 做出来的东西

- `src/lib/wiki/retrieve.ts`——**纯函数**，不读文件系统、不联网。
  中文二元组 + 西文词，idf 加权，两道闸。
- `scripts/wiki-ask.mjs`——把问题编译成 context pack：文档 ID、小节、
  来源版本、复核状态、与主命中的关系路径。**只挑材料，不下结论。**
- `knowledge/questions.md` + `scripts/check-questions.mjs`——19 条金标，
  五类：精确事实 / 跨文档组合 / 冲突 / 过期 / **无答案**。

### 过程中被实测推翻的四次

1. **`idf` 的方向是反的。** 写作 `df.get(term) ?? docCount`，于是
   「从未出现」被当成「出现在所有文档里」。症状：问「Webmention 是怎么实现的」，
   `webmention` 权重被压到最低，而「怎么实现」命中了内容协商页的小节标题
   ——**检索器用问题的虚词回答了问题的实词**，还报告「有依据」（覆盖度 0.53）。
2. **闸二查错了地方。** 最早查「返回的那几段里有没有」，于是
   `tailwind`（在 letterpress 的「不做什么」一节里）没排进前几段，
   一个**答得出来**的问题被判成「没依据」。判据改成「整个知识层里有没有」。
3. **覆盖度该按文档合并，不该按段落取最大。** 答案横跨同一页两节时，
   逐段算两段都不够门槛——而「这一页答得出来」是**文档级的事实**。
4. **接缝词不需要词典也能认出来。** `junctionsOf()`：
   两个字各自都出现在别的、语料里存在的二元组中（`式方` ← `样式`+`方案`），
   它就没有自己的字符。这一条把虚词表要扛的压力卸掉了一大半。

### 负向验证：6 次注入，5 次变红

| 注入 | 结果 |
|---|---|
| 去掉实词闸 | 红 2 条 |
| 门槛归零 | **不变红** |
| 接缝识别失效 | 红 1 条 + 1 条已知局限翻绿 |
| 文档级合并退回逐段 | 红 1 条 |
| 中文按单字切 | 红 3 条 |
| idf 方向反回去 | 红 1 条 |

**「门槛归零」那一行不变红，是这一轮最有价值的发现**：闸一当前
**没有被任何一条金标量到**，实词闸先一步拦住了那些查询。
一个没被量过的闸，和没有这个闸是一回事。它改由
`src/lib/wiki/retrieve.test.ts`（24 项）单独覆盖，并在金标文件与 README 里
**显式登记**——不假装金标全绿就等于两道闸都验过了。

### 已知局限（登记在案，不修）

中文提问一个语料里真的没有的主题，**可能仍然给出「有依据」**。
「评论区」这种真缺席的词，和「式方」这种分词副产物，**在二元组下结构完全一样**。
金标里「这个主题支持哪些语言包」就是这一条，写成 `已知局限：` 行、
不计为失败；**若它哪天翻绿，检查会报错提醒更新文档**——双向可检。
要修得靠词典或向量，而向量会摧毁「离线、可复现、无 Key」这个立身之本。

### 未决

1. **闸一没有金标**（见上）。要补得先找到「所有实词都在语料里、
   却没有一页答得出来」的**自然**问句——6 页语料上没找到，
   硬造一个就是凑用例。
2. **`knowledge/questions.md` 里没有「多跳」类。** 现在最深到一跳邻居
   （`wiki:impact` 那套），跨两跳的推理路径没有金标。
3. `llm-wiki.md` 的「已实现 / 计划实现」表**有三行已经过期**
   （来源登记、引用与复核字段、影响分析脚本都已落地）。
   金标里「来源登记与版本固定实现了吗」只保证**检索捞得到那一页**，
   捞到了也还是错的——**这是下一步 `verify:` 声明要解决的事**。

---

## 2026-09-23 · 影响分析 `wiki:impact`

**动机**：来源登记做出来之后，下一个问题自然是「来源出了新版，哪些页面要动」。
在此之前**这个问题没有答案**——引用只是一串链接、不带版本，
所以「引了这个来源」与「引了这个来源的**这一版**」分不开。

### 它第一个抓到的真东西

```
① 直接引用者（1）      cjk-typography · WD-20240312 · §5.1.1
② 可能受影响者（1）    design-tokens（经 cjk-typography）
③ 仓库辅助载体（2）    docs/cjk-typography.md
                      src/content/posts/cjk-web-typography.md
```

**③ 里那篇文章**引用了同一个规范，但因为它不是知识层条目、
不在来源登记的范围里，**此前没有任何地方把它和这个来源关联起来**。
"不在发布集合里"恰好是它最容易漏的原因。

### 设计取舍

- **三组互不重叠**（已在①的不再进③）：重叠会让人以为"还有别的地方要改"，
  而那正是这个工具要消除的困惑。
- **不联网**：只回答"已登记的这一版被谁引用了"，不去看远端有没有新版——
  那是另一件事（要网络、要登录、也不可靠）。
- **不改文件**：只出清单。应用修改要校验生成时的 hash（下一步的事）。
- **无引用时照常输出三组并标注「（没有）」**，而不是报错或空转。

### 顺带做的重构

维护脚本要读 frontmatter，而那套解析此前只住在 `remark-wikilink.ts` 里。
**在第二个地方重写就是三份实现**——抽成 `src/lib/wiki/frontmatter.ts`，
没有运行时依赖，脚本与插件都能 import。过程中踩了两个坑：
`collectFiles` 忘了跟着搬（报 `is not defined`，够直白）；
以及 `astro.config.mjs` 从一个**只导入、不再导出**的模块里 import
`collectFrontmatterProblems`，报错是 `not a function`，看不出是 import 错了地方。

### 未决问题

1. **`wiki:impact` 没有自动化测试。** 它是脚本、要读文件系统，
   目前只手工验过三种情形（有引用/无引用/多版本）。
2. **`knowledge/questions.md`（评测问题集）还没建。** 路线图阶段 3 的第二半——
   而"可评测"正是这一整套东西与"看起来在工作"的分界。
3. 前面几条未决仍未动：只有 2 页标了来源、`stale` 状态无端到端用例、
   文章 `updated` 与知识页 `review` 是两套时间概念。

---

## 2026-09-23 · 迭代 C：来源登记与复核状态机（试点 2 页）

**动机**：路线图阶段 2。在此之前页面上的「依据」只是一串链接——没有版本、
没有定位。规范出新版时没人知道哪些页该复查；读者点过去看到的是**今天的**规范。

### 做了什么

- `knowledge/sources/` 登记 5 个**真实**来源，用带日期的固定 URL。
- wiki schema 加可选的 `sources` / `review`；`Doc` 加 `wikiKind`。
- 构建期三条校验：来源解析得到（`unknown-source`）、reviewed 必须有
  日期与摘要、**摘要对不上就是 stale 并中止构建**（`review-stale`）。
- `src/components/Provenance.astro`：默认收起，不显示摘要值，无来源不渲染。
- `scripts/wiki-review.mjs`：取当前摘要，**不自动写回**。

### 排查「两边摘要不一致」——四处口径差异，全是看不出来的那种

症状是两页都报 stale，看起来像机制坏了。实际是四个独立的口径问题：

1. **`body` 少一个 `trim`**：Astro 的 `entry.body` 去过首尾空白，
   文件里切出来多两个换行。2166 vs 2168 字符。
2. **`kind` 取错字段**：`Doc.kind` 是**文档类型**（恒为 wiki），
   frontmatter 的 `kind` 是**知识类型**。摘要要后者，而 `Doc` 里根本没有它。
   → 补了 `wikiKind`。第一版用的前者，于是知识类型怎么改摘要都不变。
3. **键名错了**：脚本传 `related`，`Doc` 的字段叫 `declaredRelations`。
   `?? []` 静静兜成空数组——摘要照样算得出，只是永远对不上。
4. 导出 `digestInput`，让下次**能 diff 字符串而不是猜哈希**。

**教训**：逐项猜是猜不出来的。最后是靠"让构建打印它实际看到的 body 与字段"
定位的——四个问题里有两个是读代码看不出来的。

### 顺带：新检查抓到两个真 bug

`--color-link` / `--color-link-hover` **只有使用、从未定义**（「复制 markdown」
按钮）。CSS 自定义属性找不到时**整条声明被静默丢弃**，而 `astro check` 0 错误、
196 项契约全过、构建成功。新增 `css-tokens.test.ts` 钉住这类问题。

### 未决问题

1. **只有 2 页标了来源。** 其余 4 个知识页没有来源也没有复核状态——
   按路线图那是刻意的（"先做小范围可选增强"），但**没有门禁要求它们补**。
2. **`stale` 状态没有端到端用例。** `review-stale` 是构建期**报错**，
   而 schema 里还有一个可以手写的 `status: stale` ——那条路径没人走。
3. **文章的 `updated` 与知识页的 `review` 是两套时间概念**，
   目前没有把它们放在一起看过。
4. 来源区块只进 HTML、不进 markdown 孪生。**这是有意的**（机器出口拿的是
   正文），但意味着"HTML 与 NDJSON 三端一致"这条**只对正文成立**。

### 验证

`npm run verify:all` 全绿：100 文件类型检查 0 错、**324 项单测**、196 项契约、
条数对账、搜索、站内链接与锚点、草稿扫描 77 个文本产物、可复现、子路径、格式。

---

## 2026-09-23 · 迭代 B：统一解析与身份（三处）

**动机**：路线图阶段 1 的四件事——「唯一的解析器」「内容图与导出扫描统一」
「校验锚点与冲突」「draft/private 泄漏测试」。这一轮做了前三件里的三处。

### ① frontmatter 有两套解析，且第二套会静默解析错

`remark-wikilink.ts` 里有一个**手写的 YAML 子集解析器**（注释写着
「简单键值，不需要完整解析」），只服务于 `[[链接]]` 的查找表。

与真实 YAML 逐例比对：**12 个边界用例，6 个分歧**（块标量 `|` `>`、
值写在下一行、行尾注释、双引号转义、单引号 `''`）。
现有内容一条都不触发（11 文件 × 6 字段 = 66 次比对，0 分歧）——
所以是**潜在风险**，而下一位作者写 `title: >` 就会静默踩中。

**第一版修法是错的。** 改成越界就抛，清缓存后注入 `title: >`：

    错误打印 22 次；页面退化（链接 4→3）；Build exit code: 0

**在 remark 管道里抛错，Astro 不会让构建失败**——那比"链接静默指错"更糟。
正解是挪到 `astro.config.mjs` 的**加载期**：什么都不渲染，抛出去就是构建失败。
同一个注入现在 exit 1、指出文件与字段、一个页面都没渲染。

### ② 同名标题按遍历顺序任选一个

`buildGraph` 与 `buildLookup` 都是 `if (!has(title)) set(title, slug)`。
调换输入顺序，同一个 `[[Shared]]` 分别指向 a 和 b，而**两次 lint 都是 0 错误**。

改成「同名就不注册」：用了歧义标题 → error 并列出全部候选；
存在但没被引用 → warn。两处查找表行为一致，不会再各算各的。
端到端验证：把一页标题改成另一页的，构建 exit 1、6 条 `ambiguous-wikilink`。

### ③ 锚点从没被校验过

`[[target#missing-heading]]` 在目标页存在时，`broken` 与 lint **都是空的**。
「详见第 3.2 节」可以指向不存在的小节——**静默跳错位置**。

新增 `scripts/check-anchors.mjs`，在**产物上**查而不是从源码再推一遍：
小节 ID 是渲染期 `github-slugger` 生成的（重复标题带 `-1` 后缀），
从源码推算等于再造第三套解析器。全站 32 页、134 个锚点全部可解析。

### ④ 草稿泄漏扫描有个无声的白名单

`check-formats.mjs` 的草稿探针本来就不弱（唯标记 + 遍历 dist + 清单 + 全量导出），
但它是**按扩展名白名单跳过**的：只扫 `html|md|txt|xml|json|ndjson`。

白名单当时的判断是对的，失败方式却是无声的——将来多一个出口
（换扩展名、加一种导出格式），泄漏检查会**静默地不覆盖它**，
而扫描照样打印「草稿没有进入任何文本出口」。

改成扫全部文件、按 UTF-8 试解码，解不出的跳过但**把跳过数报出来**：

    旧白名单：65 个    新写法：77 个文本 + 30 个二进制跳过

新增覆盖的 12 个正是最可能藏内容的：`pagefind/*.js`（UI / worker）、
`_astro/style.*.css`、`favicon.svg`、`_headers`。

边界写进注释：二进制搜不出明文标记，这是**方法的边界不是保证**；
索引那侧靠另一条性质兜住——索引只能收录实际生成的页面，而草稿页根本没生成。

### ⑤ 语义正文摘要

新增 `src/lib/wiki/digest.ts`。覆盖 kind / title / summary / body / declaredRelations，
**不覆盖** slug（搬家不是改内容）、draft、以及将来的 `review` 字段本身
（自引用会让"标记已复核"这个动作自己改变摘要）。

归一化三条：行尾统一 LF（否则 Windows 检出与 Linux 检出永远是两个值）、
Unicode 归一 NFC、关系排序去重。

18 条用例分两个方向：**该变的必须变**、**不该变的必须不变**。
金标值用**独立的 Python 实现**复算比对过——不是拿被测函数自证。

### 未决问题

1. **`slug` 字段没有同样的越界检查。** 现在 `collectFrontmatterProblems`
   扫的是 `title` 与 `slug` 两个字段，但**其它字段**（`summary`、`updated`…）
   仍由 Astro 用真 YAML 解析——这一层没有分歧风险，因为 remark 根本不读它们。
2. **歧义标题的 warn 没有出现在构建输出里。** lint 的 warn 会打印，
   但没有针对"存在歧义标题"的端到端验证。
3. **draft/private 泄漏测试**（阶段 1 第四件）本轮没做。现有 `verify:formats`
   覆盖了草稿隔离的一部分，但没有对**全部**公开载体逐个断言。

### 验证

`npm run verify:all` 全绿：94 文件类型检查 0 错、**300 项单测**、196 项契约、
条数对账、搜索、**锚点**、时钟、子路径、格式。
三个提交：`31d9941`、`e8ffc6f`（含一次作者名笔误的修正 → `591cf48`）、`8f21f28`。

---

## 2026-09-23 · 阶段 0 收口：估算 vs 实测、活链接、一条核不掉的引用

**动机**：路线图阶段 0 的退出条件里有三项上一轮没做完——
估算 token 的表述、W3C 固定版本、以及那六项事实问题中的 Ahrefs 一条。

### 来源与版本

| 来源 | 版本 | 用于 |
|---|---|---|
| `tiktoken`（`o200k_base`） | 本地安装，2026-09-23 | 真实 token 用量对照 |
| [Google Search Central, *AI features and your website*](https://developers.google.com/search/docs/appearance/ai-features) | 在线版，2026-09-23 取 | llms.txt 那条的替代依据 |

### 改了什么

**① 估算被当成了实测。** 站点自己的「token 节省率」用的是
`estimateTokens`——一个刻意可解释的启发式（CJK 1 字 1 token、其余 4 字符 1 token），
而 README 与文档写的是「Measured saving」「实测」「真实数字」。

2026-09-23 装 `tiktoken` 用 `o200k_base` 逐页复算：

| 页面 | HTML 估算 / 真实 | MD 估算 / 真实 | 节省 估算 / 真实 |
|---|---:|---:|---:|
| /markdown-for-agents/ | 6,214 / 6,902（−10.0%） | 2,390 / 2,413（−1.0%） | 61.5% / **65.0%** |
| /cjk-web-typography/ | 7,614 / 8,876（−14.2%） | 2,962 / 3,201（−7.5%） | 61.1% / **63.9%** |
| /wiki/content-negotiation/ | 3,681 / 3,963（−7.1%） | 1,106 / 1,103（+0.3%） | 70.0% / **72.2%** |

**② 那个函数的注释在说反话。** `estimateTokens` 的注释写着
「宁可偏保守（**高估**），也不要让 agent 以为塞得下」——实测它**一直在低估**
（HTML 侧稳定偏低 7–14%），也就是它声称要避免的那一侧。
成因是中文：真实词表会把常见双字词合成一个 token，而「1 字 1 token」把它们各算一个。

**③ W3C 链接是活链接。** `/TR/css-values-4/` 会随规范修订移动，半年后回看
不知道引的是哪一版。已换成带日期的固定 URL（W3C 自己推荐的引用方式）：
`/TR/2024/WD-css-values-4-20240312/`、`/TR/2026/WD-css-fonts-4-20260913/`，
三处逐个 HTTP 200 验过。共替换 14 处。

**④ 一条核不掉的引用，删掉。** 原文写「Ahrefs 2026 年 5 月实测 **137,210 个域名：
97% 的 llms.txt 从未被请求过**」。核实过程：
按几种常见路径试 `ahrefs.com/blog/*` **全部 404**；
Ahrefs 自己的 sitemap **没有收录博客**；两个搜索引擎都取不到可解析结果。
**查不到就不留着**——精确到个位数、读者却无从核对，只会因为"看起来很硬"被转引。
换成能点开的 Google 原文（且措辞更准：原文是「**不需要**」，不是「不支持」）。

### 未决问题

1. **`estimateTokens` 要不要改成真的保守？** 现在没改——没有证据表明有消费方
   依赖 `x-markdown-tokens` 做硬约束，而改它就会动到对外行为。先记录，
   等真实失败样本。**但没有实测支撑的"保守"说法已经删掉了。**
2. 真实词表对照依赖 `tiktoken`（Python），**没有进 CI**——它是本地测出来的，
   项目本身不引分词器依赖。所以这张表是快照，不是每次构建复算的值。
3. 上一轮列的「1 汉字 ≈ 2ch」「跨平台字重」仍未实测，不变。

### 验证

`npm run verify` 全绿（196 项契约）。**估算数字按本次真实结果同步了三处**
（`docs/content-negotiation.md`、`README.md` ×2、`accept.ts` 注释），
又是门禁抓出来的——每次改文章都会动到这组数。


---

## 2026-09-22 · M0：中文排版的一组算式与归因错误

**动机**：同一组结论散在 6 个载体里，其中两处论证是错的，而现有的检查
（lint / 196 项契约 / 272 项单测）**全都测不到**——它们检查结构，不检查事实。

### 来源与版本

| 来源 | 固定版本 | 用于 |
|---|---|---|
| [CSS Values and Units Level 4 §ch](https://www.w3.org/TR/2024/WD-css-values-4-20240312/#ch) | W3C Working Draft, 12 March 2024 | `ch` 的定义 |
| [CSS Fonts Module Level 4](https://www.w3.org/TR/2026/WD-css-fonts-4-20260913/#font-style-matching) | W3C Working Draft, 13 September 2026 | 字重匹配（§2.2.2）与伪合成开关 |
| [RFC 9110 §12.5.1](https://www.rfc-editor.org/rfc/rfc9110.html#section-12.5.1) | RFC 9110（STD 97） | Accept 的优先级规则 |
| [Karpathy 的 llm-wiki gist](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) | gist，2026-04 | 模式定位 |

取固定的带日期 URL 而不是 `/TR/xxx/`：后者是"最新版"，会随规范修订移动，
半年后回看不知道当时引的是哪一版。

### 影响清单

**方案里列了 4 处载体，实际是 6 处。** 漏掉的两处是靠全库检索才发现的：

| # | 载体 | 原文 | 处置 |
|---|---|---|---|
| 1 | `src/content/posts/cjk-web-typography.md` | 行宽算错 + 字重归因错 | 改写 + 勘误 |
| 2 | `src/content/wiki/cjk-typography.md` | 同上 | 改写 + 勘误 |
| 3 | `docs/cjk-typography.md` | 行宽算错（字重已对） | 改写 + 勘误 |
| 4 | `src/styles/tokens.css` 注释 | 行宽算错 | 改写 |
| 5 | **`docs/compare.md`** | 行宽算错（**方案未列**） | 改写 |
| 6 | **`README.md`** | 行宽算错（**方案未列**） | 改写 |

另有第 7 处不是排版而是归因：`src/lib/negotiate/accept.ts` 的文档注释
**把平局规则列在「依据 RFC」之下**——内容协商那篇文章的错误就是从这儿来的。
只改文章不改这里，就是"同一个事实有几个表面、只改一处等于没改"。

### 改了什么

**行宽（算错 + 结论错）**

原话：「一个汉字大约等于两个 `ch`。所以按 66ch 排中文，每行会排出约 130 个汉字
——远超出中文舒适的 30 到 40 字。」

- 按它自己的前提应当**除以 2 得 33**，而不是乘以 2 得 130；
- 33 本来就落在它自己说的 30–40 里，所以「远超」也不成立；
- 被这句话遮住的事实：**66ch ≈ 33em，与本题采用的 34em 宽度几乎相同**。
  这个改动换的是**单位的可预测性**（`em` 等于字号、与字体无关），不是行宽。

**字重（归因错）**

原话：「写 `font-weight: 600` 时浏览器找不到 600，就伪合成一个……正确做法：标题字重写 500。」

- 伪合成的开关是 `font-synthesis-weight`，**不是字重数值**；
  写一个字体没有的字重时，规范要求的是**去找最接近的真实字重**；
- 本站 `base.css` 关掉合成后标题用 600：拉丁拿到 Archivo 的真实 600，
  中文落到 700。**写 500 反而会让中文掉到 400**——比 600 更糟。
- 顺带：`base.css` 的注释里**早就把对的写下来了**，是文章没跟上。

**规范归因**

内容协商那篇原先写「这时按 RFC 的规定，服务端应当认为靠前的那个优先」。
RFC 9110 §12.5.1 规定的是按**具体程度**排优先级，对 q 值相同的只说是
*equally preferred*，**没有给顺序**。所以「并列时靠前优先」是本地策略，
已在文章与 `accept.ts` 注释里都改成这个说法。

**llm-wiki.md**

- posts 原被列为「不可变的输入」，但它们**会被修订**（这篇记录本身就是证据）；
- 「RAG 永不改变、永不连接」太绝对——GraphRAG 就会组织关系与预计算摘要；
- 「一份来源可能改动 10 到 15 个页面」**没有出处**，标注为未核实；
- 补了 gist 的**真实链接**（原先只有文字，`grep -c gist.github.com` 是 0）；
- 补一张「已实现 / 计划实现」表——此前通篇以"本项目怎么做"的口吻描述，
  读者分不清哪些是真的、哪些是打算做的。

### 未决问题

1. **Ahrefs 那 97% 的引用没有可点开的来源。** 试过几种 ahrefs.com/blog 路径都是 404。
   已在文章里标注「待核验，不要转引」。要么找到确切地址，要么撤掉该句。
2. **「1 汉字 ≈ 2ch」仍是粗估。** 严格值取决于字体，本轮没有对每个候选字体实测。
   文章已改成"粗估"措辞，但没有给出实测表。
3. **跨平台字重结论未实测。** 「苹方/微软雅黑只有 400 与 700」来自 `base.css` 的既有注释，
   本轮**没有**在这些系统上实测渲染结果。文章已避免声称跨平台实测结论。
4. **`ch` 在几个候选字体下的真实宽度没有测量**，所以「66ch ≈ 33em」是推算而非实测。

### 验证

- `npm run verify:all` **全部通过**：类型检查 92 文件 0 错误、272 项单测、
  196 项契约、条数对账（272 一致）、搜索/时钟/子路径/格式检查。
- 本条的第一次提交（`863ad4f`）之后补了 `updated: 2026-09-22`——那个字段进
  JSON-LD 的 `dateModified`，页面因此多一行「修订于…」，
  `/markdown-for-agents/` 的 token 由 5857 涨到 5894（节省率 64.0% → 64.2%）。
  **这也是门禁抓的**，不是我看出来的。
- **token 节省率按本次真实结果同步**：两篇被改的文章变长，节省率从
  68.6% / 69.7% 降到 **64.2% / 62.0%**（第三篇未动，仍是 70.0%）。
  这是门禁抓出来的，不是我发现的——`verify` 会拿文档里的数字与实测比对。
  已在 `docs/content-negotiation.md` 与 `README.md` 同步，并写明原因。
- 改动只碰内容与注释，**没有改任何样式数值**：`--measure` 仍是 `34em`。

### 复核结论

六处载体的**结论都保持不变**（用 `em`、标题用 600），改的是**论证与依据**。
这说明原来的取值是对的，错的是解释它的那套说法——
而错的说法比错的取值更危险，因为它会被照抄到别处。

**这条记录本身就是 M0 的产出**：在没有建任何 CLI 的情况下，
完整走了一遍「来源 → 影响清单 → 修订 → 复核 → 验证」。
